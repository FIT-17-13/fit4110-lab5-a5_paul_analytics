# Reports

Chạy `npm run test:compose` sau khi stack Docker Compose healthy để tạo:

- `newman-lab05-compose.xml`
- `newman-lab05-compose.html`

Ảnh chụp `docker compose ps`, `/health` và kết quả Newman có thể đặt trong thư mục này trước khi nộp.
